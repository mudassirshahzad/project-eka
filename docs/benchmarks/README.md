# Benchmark Reports

This directory contains benchmark reports and performance measurements for Project EKA.

Benchmarks are produced by `IngestionBenchmarkService` and cover per-phase ingestion timing (chunk, embed, index, persist) and throughput metrics (chunks/sec, vectors/sec).

No benchmark reports have been recorded yet. Reports will be added as the platform matures.
